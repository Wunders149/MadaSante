# Server port (Render sets PORT automatically)
PORT=3001

# JWT signing secret — replace in production
JWT_SECRET=change-me-in-production

# Optional: override SQLite database file location
# DB_PATH=/var/data/madasante.db